package f.g.c.package_8;

import f.g.c.d.nf;
import java.util.Comparator;

public class NodeComparator
  implements Comparator<nf<T>>
{
  public NodeComparator(Jc.a paramA) {}
  
  public int compare(PeekingIterator paramPeekingIterator1, PeekingIterator paramPeekingIterator2)
  {
    return cmp.cmp.compare(paramPeekingIterator1.peek(), paramPeekingIterator2.peek());
  }
}
