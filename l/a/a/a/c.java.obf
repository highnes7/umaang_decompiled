package l.a.a.a;

import java.util.concurrent.CountDownLatch;
import java.util.concurrent.atomic.AtomicBoolean;

public class f
  implements k
{
  public final CountDownLatch a = new CountDownLatch(b);
  
  public f(g paramG, int paramInt) {}
  
  public void a(Exception paramException)
  {
    g.b(c).a(paramException);
  }
  
  public void a(Object paramObject)
  {
    a.countDown();
    if (a.getCount() == 0L)
    {
      g.a(c).set(true);
      g.b(c).a(c);
    }
  }
}
